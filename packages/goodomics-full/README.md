# goodomics

Default full Goodomics distribution.

This package depends on `goodomics-core` with the server, dashboard, local SQLite,
reporting, and table extras enabled. Install `goodomics-core` directly for the
lightweight pipeline/reporting profile.
