source:
  id: multiqc
  kind: source
  name: MultiQC
contracts:
  - data_contract_id: multiqc:payloads
    name: MultiQC report payload tables
    data_type: contract_payload
    producer_tool: multiqc
    value_type: table
    entity_grain: run
    primary_table: contract_payloads
    physical_tables: [contract_payloads]
    query_modes: [payload]
    mcp_description: Report-level source tables and plot payloads parsed from MultiQC outputs.
    metadata_json:
      contract_scope: semantic_contract
      source_format: multiqc
